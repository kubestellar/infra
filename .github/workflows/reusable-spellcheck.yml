name: Reusable Spellcheck

on:
  workflow_call:
    inputs:
      config_path:
        description: 'Path to spellcheck configuration file'
        required: false
        type: string
        default: '.github/spellcheck/.spellcheck.yml'
      task_name:
        description: 'Task name in spellcheck config'
        required: false
        type: string
        default: 'Markdown'

jobs:
  spellcheck:
    name: Spellcheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Run spellcheck
        uses: rojopolis/spellcheck-github-actions@cebe06444da3ac5e9d3146f5f02a0e2a236f7488
        with:
          config_path: ${{ inputs.config_path }}
          task_name: ${{ inputs.task_name }}
